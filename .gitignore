# Build artifacts — regenerated by build.sh / CI
go-learn.wasm
wasm_exec.js
_site/
# element/serr sources staged from the module cache by `./build.sh stage`
wasm/runner/srcfs/

# Local tooling
.claude/
.DS_Store
